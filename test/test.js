import {Wrestler} from "../src/Wrestler";

test('should do something', () => {
    expect(1).toBe(1);
    //const wrestler = new Wrestler("poop");
    //expect(wrestler.greet()).toBe("Hello, my name is poop");
});